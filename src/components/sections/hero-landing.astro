---
import { cn } from "@/lib/utils";
import { buttonVariants } from "@/components/ui/button";
import { Icon } from "astro-icon/components";
import { siteConfig } from "@/config/site";
import { eyeIcon } from "astro-icon/components";
import React from 'react';

---

<section class="space-y-6 py-12 sm:py-20 lg:py-20">
  <div class="container flex max-w-5xl flex-col items-center gap-5 text-center">
    <a
      href="https://x.com/atl5d"
      class={cn(
        buttonVariants({ variant: "outline", size: "sm", rounded: "full" }),
    "px-4 font-bold"
      )}
      target="_blank"
    >
      <span class="mr-3">🅰️</span> @ATL5D ON{" "}
      <Icon name="twitter" class="ml-2 size-3.5" />
    </a>

    <h1
      class="text-balance font-heading text-4xl sm:text-5xl md:text-6xl lg:text-[66px]"
    > ATL5D is the Social "Rise 🆙" 🅰️conomy {" "}<span class="text-gradient_indigo-purple font-extrabold">☔ TIP</span> game
    </h1>
    <p className="max-w-2xl text-balance leading-normal text-muted-foreground sm:text-xl sm:leading-8">
      We united ATL on 2 (block) chains (Telegram + Polkadot Kusama), educating and empowering Atlanta residents to level up one ATL
      ☔ Tip at a time. This no-cap social movement is Ushering in a new blueprint to unite the states of America—starting with Atlanta
      as an A-Team, running a game of who can rep their city the hardest by showing love to the most residents.
    </p>
    <div class="flex justify-center space-x-2 md:space-x-4">
      <a
      href="https://t.me/blum/app?startapp=memepadjetton_ATL_llnJs-ref_9K5kvzkHYM"
      target="_blank"
      rel="noreferrer"
        class={cn(buttonVariants({ size: "lg", rounded: "full" }), "gap-2")}
      >
        <span>Buy ATL</span>
        <Icon name="lucide:arrow-right" class="size-4" />
      </a>
      <a
        href="https://adtc.atl5d.com" 
        class={cn(
          buttonVariants({
            variant: "outline",
            size: "lg",
            rounded: "full",
          }),
          "px-5"
        )}
      >

        <p>
          <span class="hidden sm:inline-block">ADTC</span>🃏 Trap Game{" "}
          <!-- <span class="font-semibold">{nFormatter(stars)}</span> -->
        </p>
      </a>
    </div>
  </div>
</section>
